Features:
1. Update to Read/Write config with Viper
2. Clean up config format
3. Enable dynamic config reload
4. Enable remote config support

Tests:
1. Naked binary runs and creates the necessary files with sane defaults
2. Remote configuration works as expected (can compile in remote config)
3. Adding a plugin dynamically works as expected and is served without a restart