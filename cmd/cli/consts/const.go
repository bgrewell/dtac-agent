package consts

// CliKey is a type for the key used to store the config in the context.
type CliKey string

const (
	// KeyConfig is the key used to store the config in the context.
	KeyConfig CliKey = "config"
)
